import { AuthorizationProp, RegisrationProp } from "../entities/schemes/dto/Authorization";

export default class ApiQuery{
    static enter(data: AuthorizationProp){

    }

    static register(data: RegisrationProp){
        
    }
}